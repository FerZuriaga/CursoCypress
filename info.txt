1. npm init
2. npm install cypress --save-dev -- instalacion

2.2 Npx instalacion npmjs.com/package/npx
3. npm install -g npx
2.1 npx cypress install -force (por si no te llega a andar) (por que pasamos a la libreria npx)
3. node_modules\.bin\cypress open